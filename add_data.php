<?php
/*require_once 'db_conn.php';  // Подключение к базе данных

$fullname = isset($_POST['fullname']) ? $_POST['fullname'] : '';
$phone = isset($_POST['phone']) ? $_POST['phone'] : '';
$hectares = isset($_POST['hectares']) ? $_POST['hectares'] : '';
$farmname = isset($_POST['farmname']) ? $_POST['farmname'] : '';


try {
    $data = array('fullname' => $fullname, 'phone' => $phone, 'hectares' => $hectares, 'farmname' => $farmname);
    $query = $db->prepare("INSERT INTO $dbtable (fullname, phone, hectares, farmname) VALUES (:fullname, :phone, :hectares, farmname)");
    $query->execute($data);
    $result = true;
} catch (mysqlException $e) {
    print "Ошибка";
}

*/
?>